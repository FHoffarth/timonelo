# Data

- `source/` contains unmodified source material retained for local research. Its contents are ignored by Git by default.
- `processed/` contains reproducible generated artifacts. Its contents are ignored by Git by default.
- `ships/` contains versioned ship-specific definitions and provenance records suitable for review.

Source material must not be modified in place. Derived data must identify the source and deterministic process that produced it.

